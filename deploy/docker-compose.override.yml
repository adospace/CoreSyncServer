# Optional overrides — uncomment sections as needed.
#
# Usage:
#   docker compose -f docker-compose.yml -f docker-compose.override.yml up -d

services:
  coresyncserver:
    # ── HTTPS with a custom certificate ──
    # ports:
    #   - "8080:8080"
    #   - "8081:8081"
    # environment:
    #   - ASPNETCORE_URLS=http://+:8080;https://+:8081
    #   - ASPNETCORE_Kestrel__Certificates__Default__Path=/https/cert.pfx
    #   - ASPNETCORE_Kestrel__Certificates__Default__Password=your-cert-password
    # volumes:
    #   - ./certs:/https:ro

    # ── SMTP email notifications ──
    # environment:
    #   - Smtp__Enabled=true
    #   - Smtp__Host=smtp.example.com
    #   - Smtp__Port=587
    #   - Smtp__FromAddress=noreply@example.com
    #   - Smtp__ToAddress=admin@example.com
    #   - Smtp__Username=smtp-user
    #   - Smtp__Password=smtp-password
    #   - Smtp__EnableSsl=true

    # ── Maintenance tuning ──
    # environment:
    #   - Maintenance__IntervalMinutes=120
    #   - Maintenance__SyncSessionRetentionDays=30
    #   - Monitor__IntervalMinutes=10

    # Placeholder to keep the file valid YAML when everything is commented out.
    labels:
      com.coresyncserver.override: "true"
